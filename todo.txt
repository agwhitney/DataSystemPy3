1) Fix ttyUSB mounting location for instruments.
    https://stackoverflow.com/questions/24714241/ttyusb-numbers-are-changing-after-reboot
    This seems to work if things don't move (so maybe it isn't different?).


2) Script to get external master timestamp to sync system time with offset.


3) Should data org be changed a little? Think of a downlink rsync via Starlink, and think of the hammr-hd data as well.


4) Thermistors.csv should be copied to data output for decoupled access in L0b processing.


5) Need a script that starts this software and hyperspectral software together.
    Data org, per point 3.
    User-facing configuration (as opposed to client.json)?
    
    1/2/26 -- run_system.sh works.
        JPL savedir could be handled by STDIN. CSU is all config'd in this project.